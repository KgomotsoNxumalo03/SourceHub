import { ActivityIndicator, Pressable, Text, TextInput, View, type PressableProps, type TextInputProps } from "react-native";
import { colors, styles } from "@mobile/theme";

export function Button({ label, loading, variant = "primary", ...props }: PressableProps & { label: string; loading?: boolean; variant?: "primary" | "secondary" }) { return <Pressable accessibilityRole="button" accessibilityLabel={label} style={({ pressed }) => [variant === "primary" ? styles.primaryButton : styles.secondaryButton, pressed && { opacity: 0.75 }]} {...props}>{loading ? <ActivityIndicator color={variant === "primary" ? colors.surface : colors.blue} /> : <Text style={variant === "primary" ? styles.buttonText : styles.secondaryText}>{label}</Text>}</Pressable>; }
export function Field({ label, ...props }: TextInputProps & { label: string }) { return <View style={{ gap: 7 }}><Text style={styles.label}>{label}</Text><TextInput accessibilityLabel={label} placeholderTextColor="#94A3B8" style={styles.input} {...props} /></View>; }
export function StatusBadge({ label, tone = "default" }: { label: string; tone?: "default" | "success" | "warning" | "danger" }) { const backgroundColor = tone === "success" ? "#E5F6EE" : tone === "warning" ? "#FFF3D6" : tone === "danger" ? "#FFF0EE" : "#E6EEF9"; const color = tone === "success" ? colors.success : tone === "warning" ? colors.warning : tone === "danger" ? colors.danger : colors.blue; return <View style={[styles.badge, { backgroundColor }]}><Text style={[styles.badgeText, { color }]}>{label}</Text></View>; }
export function EmptyState({ title, description }: { title: string; description: string }) { return <View style={[styles.card, { alignItems: "center", paddingVertical: 28 }]}><Text style={styles.cardTitle}>{title}</Text><Text style={[styles.subtitle, { textAlign: "center" }]}>{description}</Text></View>; }
