import { Tabs } from "expo-router";
import { Text } from "react-native";

import { useAuth } from "@mobile/core/auth-context";
import { hasMobilePermission, roleMode } from "@mobile/core/mobile-core";
import { colors } from "@mobile/theme";

export default function TabsLayout() { const { user, offline } = useAuth(); const mode = roleMode(user); return <Tabs screenOptions={{ tabBarActiveTintColor: colors.blue, tabBarInactiveTintColor: colors.muted, tabBarLabelStyle: { fontSize: 10, fontWeight: "700" }, headerShown: false, tabBarHideOnKeyboard: true }}><Tabs.Screen name="index" options={{ title: "Home", tabBarIcon: ({ color }) => <Text style={{ color }}>⌂</Text> }} /><Tabs.Screen name="tickets" options={{ title: "Tickets", href: mode === "employee" ? null : "/(tabs)/tickets", tabBarIcon: ({ color }) => <Text style={{ color }}>▣</Text> }} /><Tabs.Screen name="assets" options={{ title: "Assets", href: hasMobilePermission(user, "mobile.assets.view") ? "/(tabs)/assets" : null, tabBarIcon: ({ color }) => <Text style={{ color }}>▤</Text> }} /><Tabs.Screen name="attendance" options={{ title: "Attendance", href: hasMobilePermission(user, "mobile.attendance.use") ? "/(tabs)/attendance" : null, tabBarIcon: ({ color }) => <Text style={{ color }}>◷</Text> }} /><Tabs.Screen name="tasks" options={{ title: "Tasks", href: hasMobilePermission(user, "mobile.projects.use") ? "/(tabs)/tasks" : null, tabBarIcon: ({ color }) => <Text style={{ color }}>✓</Text> }} /><Tabs.Screen name="knowledge" options={{ title: "Knowledge", href: hasMobilePermission(user, "mobile.knowledge.use") ? "/(tabs)/knowledge" : null, tabBarIcon: ({ color }) => <Text style={{ color }}>?</Text> }} /><Tabs.Screen name="notifications" options={{ title: offline ? "Offline" : "Alerts", tabBarIcon: ({ color }) => <Text style={{ color }}>•</Text> }} /><Tabs.Screen name="profile" options={{ title: "Profile", tabBarIcon: ({ color }) => <Text style={{ color }}>○</Text> }} /></Tabs>; }
