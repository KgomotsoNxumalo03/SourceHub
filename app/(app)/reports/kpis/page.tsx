import { requirePermission } from "@/lib/auth";
import { getReportingHome } from "@/lib/reporting";
import { Badge, Card, CardContent, PageHeader, Table, TableBody, TableCell, TableHead, TableHeadCell, TableRow } from "@/components/ui";
export default async function KpiDefinitionsPage() { await requirePermission("reports.view"); const home = await getReportingHome(); return <div className="space-y-6"><PageHeader eyebrow="Reporting governance" title="KPI definitions" description="Every KPI exposes its source, calculation, inclusion rules, unit, refresh expectation and version." /><div className="space-y-4">{home.kpis.map((kpi: any) => <Card key={kpi.key ?? kpi.id}><CardContent><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 className="text-lg font-semibold">{kpi.name}</h2><p className="mt-1 text-sm text-slate-600">{kpi.description}</p></div><Badge>v{kpi.version}</Badge></div><Table className="mt-4"><TableHead><TableRow><TableHeadCell>Calculation</TableHeadCell><TableHeadCell>Date basis</TableHeadCell><TableHeadCell>Unit</TableHeadCell><TableHeadCell>Refresh</TableHeadCell></TableRow></TableHead><TableBody><TableRow><TableCell>{kpi.calculation}</TableCell><TableCell>{kpi.dateBasis}</TableCell><TableCell>{kpi.unit}</TableCell><TableCell>{kpi.refreshFrequency}</TableCell></TableRow></TableBody></Table></CardContent></Card>)}</div></div>; }
