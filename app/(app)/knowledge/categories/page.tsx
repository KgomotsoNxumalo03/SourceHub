import { requirePermission } from "@/lib/auth";
import { prisma } from "@/lib/db";
import { env } from "@/lib/env";
import { createKnowledgeCategoryAction } from "@/lib/actions/knowledge";
import { Button, Card, CardContent, Input, PageHeader, Select, Table, TableBody, TableCell, TableHead, TableHeadCell, TableRow, Textarea } from "@/components/ui";
export default async function KnowledgeCategoriesPage() { await requirePermission("knowledge.categories.manage"); const categories: any[] = await prisma.knowledgeCategory.findMany({ where: { workspaceId: env.DEFAULT_WORKSPACE_ID }, orderBy: [{ sortOrder: "asc" }, { name: "asc" }], take: 200 }); return <div className="space-y-6"><PageHeader eyebrow="Knowledge configuration" title="Categories" description="Keep taxonomy shallow, explicit and scoped by audience." /><Card><CardContent><form action={createKnowledgeCategoryAction} className="grid gap-3 md:grid-cols-3"><Input name="name" placeholder="Category name" required /><Input name="icon" placeholder="Icon name" defaultValue="book-open" /><Select name="area"><option>INTERNAL</option><option>CLIENT</option><option>PUBLIC</option></Select><Textarea name="description" placeholder="Description" className="md:col-span-2" /><Input name="sortOrder" type="number" defaultValue="0" /><label className="flex items-center gap-2 text-sm"><input name="active" type="checkbox" defaultChecked /> Active</label><Button type="submit">Add category</Button></form></CardContent></Card><Table><TableHead><TableRow><TableHeadCell>Name</TableHeadCell><TableHeadCell>Area</TableHeadCell><TableHeadCell>Active</TableHeadCell></TableRow></TableHead><TableBody>{categories.map((category) => <TableRow key={category.id}><TableCell>{category.name}</TableCell><TableCell>{category.area}</TableCell><TableCell>{category.active ? "Yes" : "No"}</TableCell></TableRow>)}</TableBody></Table></div>; }
