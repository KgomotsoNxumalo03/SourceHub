import { requirePermission } from "@/lib/auth";
import { createKnowledgeImportAction } from "@/lib/actions/knowledge";
import { Button, Card, CardContent, Input, PageHeader, Select, Textarea } from "@/components/ui";
export default async function KnowledgeImportPage() { await requirePermission("knowledge.import.manage"); return <div className="space-y-6"><PageHeader eyebrow="Knowledge operations" title="Import content" description="Imports are idempotent and always create internal drafts for review." /><Card><CardContent><form action={createKnowledgeImportAction} className="space-y-4"><Input name="sourceName" placeholder="Source filename or system name" required /><Input name="idempotencyKey" placeholder="Unique import key" required /><Select name="format"><option value="markdown">Markdown</option><option value="plain">Plain text</option><option value="html">Approved HTML</option><option value="csv">CSV</option></Select><Textarea name="content" className="min-h-80 font-mono text-xs" required /><Button type="submit">Create draft import</Button></form></CardContent></Card></div>; }
