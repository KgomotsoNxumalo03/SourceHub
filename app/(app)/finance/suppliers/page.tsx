import { requirePermission } from "@/lib/auth";
import { prisma } from "@/lib/db";
import { env } from "@/lib/env";
import { createSupplierAction } from "@/lib/actions/finance";
import { Button, Card, CardContent, Input, PageHeader, Table, TableBody, TableCell, TableHead, TableHeadCell, TableRow } from "@/components/ui";
export const dynamic = "force-dynamic";
export default async function SuppliersPage() { const actor = await requirePermission("suppliers.view"); const suppliers: any[] = await prisma.supplier.findMany({ where: { workspaceId: env.DEFAULT_WORKSPACE_ID }, orderBy: [{ name: "asc" }], take: 100 }); return <div className="space-y-6"><PageHeader eyebrow="Finance management" title="Suppliers" description="Maintain supplier master data and verification state." /><Card><CardContent><form action={createSupplierAction} className="grid gap-3 md:grid-cols-3"><Input name="name" placeholder="Supplier name" required /><Input name="tradingName" placeholder="Trading name" /><Input name="email" type="email" placeholder="Email" /><Input name="telephone" placeholder="Telephone" /><Input name="paymentTermsDays" type="number" defaultValue="30" required /><Input name="currency" defaultValue={env.FINANCE_DEFAULT_CURRENCY} maxLength={3} required /><input type="hidden" name="bankingVerificationStatus" value="UNVERIFIED" /><Button type="submit" disabled={!actor.permissions.includes("suppliers.manage")}>Add supplier</Button></form></CardContent></Card>{suppliers.length ? <Table><TableHead><TableRow><TableHeadCell>Supplier</TableHeadCell><TableHeadCell>Contact</TableHeadCell><TableHeadCell>Verification</TableHeadCell><TableHeadCell>Status</TableHeadCell></TableRow></TableHead><TableBody>{suppliers.map((supplier) => <TableRow key={supplier.id}><TableCell className="font-medium">{supplier.name}</TableCell><TableCell>{supplier.email || supplier.telephone || "-"}</TableCell><TableCell>{supplier.bankingVerificationStatus}</TableCell><TableCell>{supplier.status}</TableCell></TableRow>)}</TableBody></Table> : null}</div>; }
